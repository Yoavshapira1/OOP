package filesprocessing.Sorts;

/** Interface to gather the format variables of the order parameters line in the command file. */
public interface ParametersFormat {

    /* This value represents the reversed-order sorting command. */
    String REVERSE = "REVERSE";

    /* This value represents the String that separate the input's parameters. */
    String SEPARATOR = "#";

}
